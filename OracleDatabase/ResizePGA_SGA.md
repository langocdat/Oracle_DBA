# Oracle Database 10G
1. SGA_target
2. SGA_MAX_SIZE (static parameter -> restart instance)

   `
   SGA_max_size là con số tối đa mà có thể cung cấp cho SGA
   SGA_target <= SGA_max_szie
   `
   
4. PGA_Aggregate_target
