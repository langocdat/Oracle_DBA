# 1. Check MVIEW chạy bao lâu với mode Complete

SQL>
```
SET TIMING ON;
EXEC DBMS_MVIEW.REFRESH('IMS_2017.MV_RCF_PASSENGER_TRANS', 'C');
```
- Tiếp tục check time với mode Fash Refreash
```
SET TIMING ON;
EXEC DBMS_MVIEW.REFRESH('IMS_2017.MV_RCF_PASSENGER_TRANS', 'F');
```
# 2. Khi chạy mode F có lỗi, check lỗi
SQL
```
BEGIN
    DBMS_MVIEW.EXPLAIN_MVIEW('IMS_2017.MV_PASSENGER_TRANS');
END;
/
```

- Check nội dung Query
```
SELECT QUERY FROM DBA_MVIEWS WHERE MVIEW_NAME = 'BLACKLIST_PASSENGER' AND OWNER = 'IMS_2017';
```
